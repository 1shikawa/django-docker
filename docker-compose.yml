version: '3'

services:
#  db:
#    image: postgres
#    volumes:
#      - postgresql-data:/var/lib/postgresql/data

  web:
    build: .
    volumes:
      - .:/code
    ports:
      - "8000:8000"
#    depends_on:
#      - db